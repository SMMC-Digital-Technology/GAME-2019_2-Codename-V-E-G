/**
 * A "game over" screen for the end of the game
 */
var gameoverState = {
   create: function() {
      // create the screen

      //add button
   },

   buttonClick: function() {
     game.state.start('menu');
   }
};
